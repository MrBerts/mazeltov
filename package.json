{
  "name": "mazeltov",
  "version": "1.0.0",
  "description": "solve a maze",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "multer": "^1.2.1"
  }
}
